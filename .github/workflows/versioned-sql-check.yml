name: Check and Rename SQL Migration Versions

on:
  pull_request:
    paths:
      - 'src/main/resources/database/migration_flyway/**'
      - 'src/main/resources/database/revert_flyway/**'

jobs:
  check-sql-versions:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Git user
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

      - name: Checkout PR branch locally
        run: |
          echo "Checking out PR branch ${{ github.head_ref }}"
          git checkout -B "${{ github.head_ref }}"

      - name: Run SQL version checker script
        run: bash .github/workflows/scripts/check_sql_versions.sh

      - name: Push changes if any
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Fetch remote state to compare
          git fetch origin ${{ github.head_ref }}
          
          LOCAL_HEAD=$(git rev-parse HEAD)
          REMOTE_HEAD=$(git rev-parse origin/${{ github.head_ref
